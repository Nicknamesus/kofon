# DRAFT — extracted from https://www.kofon.com.cn/products_detail/53.html
# Review before promoting to seed/product_types/xam_series.yaml
# Notes: This is a servo motor product page (XAM series), not a gearbox. Family set to 'Servo motor' as it doesn't fit the existing families. The page mentions 48V/380V voltage grades in the overview but the table only lists 220V models — only 220V entries were extracted. The page mentions 40mm-180mm flange sizes in text but the table only shows 60/80/100/130 mm — only table data used. Encoder type '23-bit multi-turn absolute optical encoder' is stated in the table header. Pole pairs (5), protection class (IP65), insulation class (F) are from the feature list. No torque, speed, or current specs are provided on the page. The variant codes (01BA0, 01AA0, 11BA0, 11AA0, 01B01, 11B01, 01B00, 11B00) are taken directly from the model numbers as listed.
code: xam_series
name: XAM Series
family: Servo motor
description: XAM series servo motors feature 5-pole-pair, low cogging torque design.
  Used with RD series servo drives to form a powerful, high-performance servo system.
  Power range 0.05 kW to 2 kW, flange sizes 60 mm to 130 mm, with multiple inertia
  and speed configurations. Available in 48 V, 220 V, and 380 V voltage grades. Equipped
  with 23-bit multi-turn absolute optical encoder. IP65 protection, Class F insulation.
spec_schema:
  frame_size_mm:
    type: integer
    label: Frame size (mm)
  voltage_v:
    type: integer
    label: Voltage (V)
  power_w:
    type: integer
    label: Rated power (W)
  with_brake:
    type: string
    label: With brake
  encoder_type:
    type: string
    label: Encoder type
  pole_pairs:
    type: integer
    label: Pole pairs
  protection_class:
    type: string
    label: Protection class
  insulation_class:
    type: string
    label: Insulation class
  variant:
    type: string
    label: Variant
