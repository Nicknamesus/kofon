# DRAFT — extracted from https://www.kofon.com.cn/products_detail/127.html
# Review before promoting to seed/product_types/k_r_series.yaml
# Notes: Single frame size KHR090 only. Expanded all listed ratios into individual SKUs (5 one-stage + 10 two-stage = 15 SKUs). The page lists "最大侧倾力矩 Max.Tilting Moment F₂QMax N 437" — the unit is labeled N but the value 437 is likely Nm (Nm is standard for tilting moment). Kept as-is from page but noted for reviewer. Rotational inertia values given in kg·cm² (kept as-is). 2-stage lubrication is "油脂润滑" (grease lubrication) vs 1-stage "油润滑" (oil lubrication). Weight differs: 1-stage = 4.8 kg, 2-stage = 6 kg. Efficiency: 1-stage = 96%, 2-stage = 94%. Max radial force and max tilting moment are shared across all ratios (single value per stage type).
code: k_r_series
name: K-R Series — Hypoid Right-Angle Gearbox
family: Bevel gear
description: Kofon K-R series hypoid right-angle gearboxes, featuring high precision
  (backlash ≤4 arcmin), high torque density, and compact right-angle design. Available
  in 1-stage (ratios 3–10) and 2-stage (ratios 12–100) configurations. The KHR090
  frame uses advanced hypoid gear technology with case-hardened alloy steel, high-capacity
  tapered roller bearings, and IP65 protection. Suitable for servo motor integration
  with customizable output dimensions.
spec_schema:
  frame_size_mm:
    type: string
    label: Frame size
  stages:
    type: integer
    label: Number of stages
  ratio:
    type: integer
    label: Ratio i
  nominal_torque_nm:
    type: number
    label: Rated output torque (Nm)
  peak_torque_nm:
    type: number
    label: Maximum acceleration torque (Nm)
  emergency_stop_torque_nm:
    type: number
    label: Emergency stop torque (Nm)
  rated_input_speed_rpm:
    type: integer
    label: Rated input speed (rpm)
  max_input_speed_rpm:
    type: integer
    label: Max input speed (rpm)
  mean_no_load_torque_nm:
    type: number
    label: Mean no load running torque (Nm)
  backlash_arcmin:
    type: number
    label: Backlash (arcmin)
  torsional_rigidity_nm_per_arcmin:
    type: number
    label: Torsional rigidity (Nm/arcmin)
  max_axial_force_n:
    type: integer
    label: Max axial force (N)
  max_tilting_moment_nm:
    type: integer
    label: Max tilting moment (Nm)
  rotational_inertia_kgcm2:
    type: number
    label: Rotational inertia (kg·cm²)
  noise_db_a:
    type: number
    label: Noise (dB(A))
  efficiency_pct:
    type: integer
    label: Full loading efficiency (%)
  operating_temp_min_c:
    type: integer
    label: Operating temperature min (°C)
  operating_temp_max_c:
    type: integer
    label: Operating temperature max (°C)
  lubrication:
    type: string
    label: Lubrication
  mounting_position:
    type: string
    label: Mounting position
  protection_class:
    type: string
    label: Protection class
  service_lifetime_h:
    type: integer
    label: Service lifetime (h)
  weight_kg:
    type: number
    label: Weight (kg)
