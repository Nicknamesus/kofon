# DRAFT — extracted from https://www.kofon.com.cn/products_detail/91.html
# Review before promoting to seed/product_types/kr090_25.yaml
# Notes: Single SKU page for the KR090-25 integrated joint module. Only one ratio (25:1) is listed. All specs taken directly from the '技术参数' table. The datasheet PDF link ('伺服行星关节模组20250914.pdf') is referenced on the page but the full URL is not directly extractable from the text; left as null. The family is classified as 'Harmonic drive / strain wave' because it's an integrated joint module with a reducer, dual encoder, and built-in drive — similar to harmonic-drive-based actuation modules. The product type code 'kr090_25' reflects the model name.
code: kr090_25
name: KR090-25 Joint Module
family: Harmonic drive / strain wave
description: The KR090-25 is an integrated joint module featuring dual encoders for
  closed-loop control, eliminating transmission chain errors. It supports multiple
  control modes (force-position hybrid, servo position, servo velocity, current/torque)
  and includes adaptive control technology, high control bandwidth, and protections
  against over-temperature, over-voltage, and overload. The module integrates a 25:1
  reducer, a brushless motor, and a built-in drive with EtherCAT communication.
spec_schema:
  ratio:
    type: integer
    label: Reducer ratio
  rated_voltage_v:
    type: number
    label: Rated Voltage (V)
  rated_current_a:
    type: number
    label: Rated Phase Current (A)
  rated_power_w:
    type: number
    label: Rated Power (W)
  rated_speed_rpm:
    type: integer
    label: Rated Speed (RPM)
  nominal_torque_nm:
    type: number
    label: Rated Torque (Nm)
  peak_current_a:
    type: number
    label: Peak Phase Current (A)
  peak_torque_nm:
    type: number
    label: Peak Torque (Nm)
  peak_speed_rpm:
    type: integer
    label: Peak Speed (RPM)
  efficiency_pct:
    type: number
    label: Efficiency (%)
  power_density_w_kg:
    type: number
    label: Power Density (W/Kg)
  torque_constant_nm_per_a:
    type: number
    label: Torque constant (Nm/A)
  weight_kg:
    type: number
    label: Weight (kg)
  backlash_arcmin:
    type: number
    label: Backlash (Arcmin)
  frame_size_mm:
    type: string
    label: Motor Size (mm)
  communication_protocol:
    type: string
    label: Communication
  baud_rate:
    type: string
    label: Communication baud rate
  has_dual_encoder:
    type: string
    label: Dual Encoder
  has_built_in_drive:
    type: string
    label: Built-in Drive
