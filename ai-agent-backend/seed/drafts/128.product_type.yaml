# DRAFT — extracted from https://www.kofon.com.cn/products_detail/128.html
# Review before promoting to seed/product_types/k_r_series.yaml
# Notes: Only one frame size (KHR160) is on this page. The product family is the K-R Series (准双曲面直角减速机K-R系列). The rotational inertia values were given in kg·cm² — kept as-is (not converted to kg·m²). The 2-stage lubrication is 'Grease lubrication' vs 1-stage 'Oil lubrication' per the table. Backlash is listed as ≤4 arcmin for all variants. Max axial force (9900 N) and max tilting moment (1692 Nm) are single values shared across all ratios. The page title says 'KHR160 减速机' but the category is K-R series. The datasheet PDF link is provided as '准双曲面直角减速机K-R系列.pdf' but no full URL was available from the page text.
code: k_r_series
name: K-R Series — Hypoid Right-Angle Gearbox
family: Bevel gear
description: Kofon K-R series hypoid right-angle gearboxes, featuring high-precision
  spiral bevel gear transmission with backlash ≤4 arcmin. Available in 1-stage and
  2-stage configurations with ratios from 3:1 to 100:1. Designed for high torque,
  low noise (≤68 dB(A)), and long service life (20,000 h). Equipped with high-strength
  tapered roller bearings for high overturning moment capacity. IP65 protection, suitable
  for any mounting position.
spec_schema:
  ratio:
    type: integer
    label: Ratio
  stages:
    type: integer
    label: Number of stages
  nominal_torque_nm:
    type: number
    label: Rated output torque (Nm)
  peak_torque_nm:
    type: number
    label: Maximum acceleration torque (Nm)
  emergency_stop_torque_nm:
    type: number
    label: Emergency stop torque (Nm)
  rated_input_speed_rpm:
    type: integer
    label: Rated input speed (rpm)
  max_input_speed_rpm:
    type: integer
    label: Max input speed (rpm)
  no_load_torque_nm:
    type: number
    label: Mean no-load running torque (Nm)
  backlash_arcmin:
    type: number
    label: Backlash (arcmin)
  torsional_rigidity_nm_per_arcmin:
    type: number
    label: Torsional rigidity (Nm/arcmin)
  max_axial_force_n:
    type: integer
    label: Max axial force (N)
  max_tilting_moment_nm:
    type: integer
    label: Max tilting moment (Nm)
  rotational_inertia_kg_cm2:
    type: number
    label: Rotational inertia (kg·cm²)
  noise_db_a:
    type: number
    label: Noise (dB(A))
  efficiency_pct:
    type: integer
    label: Efficiency (%)
  operating_temp_min_c:
    type: integer
    label: Operating temperature min (°C)
  operating_temp_max_c:
    type: integer
    label: Operating temperature max (°C)
  lubrication:
    type: string
    label: Lubrication
  mounting_position:
    type: string
    label: Mounting position
  protection_class:
    type: string
    label: Protection class
  service_lifetime_h:
    type: integer
    label: Service lifetime (h)
  weight_kg:
    type: number
    label: Weight (kg)
  frame_size:
    type: string
    label: Frame size
