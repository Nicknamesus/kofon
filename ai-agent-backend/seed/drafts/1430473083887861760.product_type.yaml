# DRAFT — extracted from https://www.kofon.com.cn/products_detail/1430473083887861760.html
# Review before promoting to seed/product_types/kr045_30.yaml
# Notes: Single SKU product page for the KR045-30 joint module. All specs were extracted from the '技术参数' (Technical Parameters) table. The product appears to be an integrated servo actuator with harmonic drive (based on the '关节模组' / joint module naming and the use of a reducer with backlash ≤8 arcmin). The family is classified under 'Harmonic drive / strain wave' as it's a compact joint module with a reducer. The datasheet PDF link is relative but the page URL is provided as source. The 'built_in_driver' spec says '可选择' (optional/selectable) — interpreted as 'Optional'.
code: kr045_30
name: KR045-30 Joint Module
family: Harmonic drive / strain wave
description: The KR045-30 is an integrated joint module (actuator module) combining
  a servo motor, harmonic drive reducer, and dual encoders for closed-loop control.
  It features a 30:1 reduction ratio, peak torque of 9 Nm, rated torque of 3 Nm, and
  supports multiple control modes including force-position hybrid, servo position,
  servo velocity, and current (torque) control. It includes adaptive control technology,
  high control bandwidth, and protections against over-temperature, over-voltage,
  and overload. The module is compact at 45 mm outer diameter × 53.5 mm thickness
  and weighs only 0.35 kg.
spec_schema:
  reduction_ratio:
    type: integer
    label: Reduction Ratio
  rated_voltage_v:
    type: number
    label: Rated Voltage (V)
  rated_current_a:
    type: number
    label: Rated Phase Current (A)
  rated_power_w:
    type: number
    label: Rated Power (W)
  rated_speed_rpm:
    type: number
    label: Rated Speed (RPM)
  rated_torque_nm:
    type: number
    label: Rated Torque (Nm)
  peak_current_a:
    type: number
    label: Peak Phase Current (A)
  peak_torque_nm:
    type: number
    label: Peak Torque (Nm)
  peak_speed_rpm:
    type: number
    label: Peak Speed (RPM)
  efficiency_pct:
    type: number
    label: Efficiency (%)
  power_density_w_kg:
    type: number
    label: Power Density (W/kg)
  torque_constant_nm_per_a:
    type: number
    label: Torque Constant (Nm/A)
  weight_kg:
    type: number
    label: Weight (kg)
  backlash_arcmin:
    type: number
    label: Backlash (Arcmin)
  motor_size_mm:
    type: string
    label: Motor Size (Diameter × Thickness, mm)
  communication_protocol:
    type: string
    label: Communication Protocol
  communication_baud_rate:
    type: string
    label: Communication Baud Rate
  double_encoder:
    type: string
    label: Double Encoder
  built_in_driver:
    type: string
    label: Built-in Driver
