# DRAFT — extracted from https://www.kofon.com.cn/products_detail/117.html
# Review before promoting to seed/product_types/kpx_s_series.yaml
# Notes: 1. The page lists "KPX-SY115 手摇" under category "手摇输入减速机KPX-S系列" (Hand-input reducer KPX-S series). Product type code chosen as 'kpx_s_series'. 2. The page has two tables: 1-stage (ratios 3-10) and 2-stage (ratios 12-64). All ratios expanded into individual SKUs. 3. The rotational inertia values for 2-stage ratios 30-60 are listed as "2" (kg·cm²) — assumed to be 2.0. 4. The page header says "KPX-SY115 手摇" — the model code is KPX-SY115. 5. Note: The page labels "最大轴向力" as "Max Radial Force" (F2AMax) and "最大径向力" as "Max Axial Force" (F2QMax) — these appear to be swapped in the Chinese labels vs English headers. I preserved the numeric values as given (6700N radial, 3300N axial) matching the English column headers. 6. The 2-stage rotational inertia unit is listed as "kgcm" (missing ²) — assumed kg·cm². 7. Datasheet PDF available at the site but no direct URL extracted; left null. 8. No CAD URLs or lead times on the page.
code: kpx_s_series
name: KPX-S Series Hand-Input Reducer
family: Planetary gearbox
description: The KPX-S series is a hand-input (manual crank) planetary reducer designed
  for applications where electric power may be unavailable or unreliable. It allows
  manual operation during power failures or in remote areas, ensuring continued equipment
  functionality. Available in 1-stage and 2-stage configurations with a wide range
  of ratios.
spec_schema:
  ratio:
    type: integer
    label: Ratio i
  stages:
    type: integer
    label: Number of stages
  nominal_torque_nm:
    type: number
    label: Rated output torque (Nm)
  peak_torque_nm:
    type: number
    label: Maximum acceleration torque (Nm)
  emergency_stop_torque_nm:
    type: number
    label: Emergency stop torque (Nm)
  input_speed_rpm_max:
    type: integer
    label: Max input speed (rpm)
  rated_input_speed_rpm:
    type: integer
    label: Rated input speed (rpm)
  backlash_arcmin:
    type: number
    label: Backlash (arcmin)
  max_radial_force_n:
    type: integer
    label: Max radial force (N)
  max_axial_force_n:
    type: integer
    label: Max axial force (N)
  rotational_inertia_kgcm2:
    type: number
    label: Rotational inertia (kg·cm²)
  noise_db_a:
    type: number
    label: Noise (dB(A))
  efficiency_pct:
    type: integer
    label: Full loading efficiency (%)
  operating_temp_min_c:
    type: integer
    label: Operating temperature min (°C)
  operating_temp_max_c:
    type: integer
    label: Operating temperature max (°C)
  lubrication:
    type: string
    label: Lubrication
  mounting_position:
    type: string
    label: Mounting position
  service_lifetime_h:
    type: integer
    label: Service lifetime (h)
  weight_kg:
    type: number
    label: Weight (kg)
