# DRAFT — extracted from https://www.kofon.com.cn/products_detail/126.html
# Review before promoting to seed/product_types/kpx_s_series.yaml
# Notes: 1. The page lists "KPX-SY142 手摇" under category "手摇输入减速机KPX-S系列" (Hand-input reducer KPX-S series). I used product_type_code 'kpx_s_series' to cover the whole series. 2. The 1-stage table has ratios 3-10 with individual torque values per ratio — expanded to 8 SKUs. 3. The 2-stage table has ratios 12-64 with individual torque values — expanded to 13 SKUs. 4. Rotational inertia values are given in kg·cm² (the page says "kgcm²" for 1-stage and "kgcm" for 2-stage — assumed same unit kg·cm²). 5. The page has a typo "Emergency sotp torque" — interpreted as "Emergency stop torque". 6. Noise for 2-stage shows "dB(AF" — likely a typo for "dB(A)", used ≤70. 7. Max radial force (F2AMax) = 9600 N, Max axial force (F2QMax) = 4800 N — these are shared across all variants. 8. Weight: 1-stage = 28 kg, 2-stage = 36.7 kg. 9. No datasheet URLs or CAD URLs provided on the page beyond a general PDF link for the series. 10. No lead time info available.
code: kpx_s_series
name: KPX-S Series Hand-Input Reducer
family: Planetary gearbox
description: The KPX-S series is a hand-input (manual crank) planetary reducer designed
  for applications where electric power may be unavailable or unreliable. It allows
  manual operation during power failures or motor faults, ensuring continued equipment
  operation in remote or emergency situations. Available in 1-stage and 2-stage configurations
  with a wide range of ratios.
spec_schema:
  ratio:
    type: integer
    label: Ratio i
  stages:
    type: integer
    label: Number of stages
  nominal_torque_nm:
    type: number
    label: Rated output torque (Nm)
  peak_torque_nm:
    type: number
    label: Maximum acceleration torque (Nm)
  emergency_stop_torque_nm:
    type: number
    label: Emergency stop torque (Nm)
  rated_input_speed_rpm:
    type: integer
    label: Rated input speed (rpm)
  max_input_speed_rpm:
    type: integer
    label: Max input speed (rpm)
  backlash_arcmin:
    type: number
    label: Backlash (arcmin)
  max_radial_force_n:
    type: integer
    label: Max radial force (N)
  max_axial_force_n:
    type: integer
    label: Max axial force (N)
  rotational_inertia_kgcm2:
    type: number
    label: Rotational inertia (kg·cm²)
  noise_db:
    type: number
    label: Noise (dB(A))
  efficiency_pct:
    type: integer
    label: Full loading efficiency (%)
  operating_temp_min_c:
    type: integer
    label: Operating temperature min (°C)
  operating_temp_max_c:
    type: integer
    label: Operating temperature max (°C)
  lubrication:
    type: string
    label: Lubrication
  mounting_position:
    type: string
    label: Mounting position
  service_lifetime_h:
    type: integer
    label: Service lifetime (h)
  weight_kg:
    type: number
    label: Weight (kg)
