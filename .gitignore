# Standalone repo for the hardcoded (single-process) demo build.
# Lives inside this tree for convenience but is managed independently.
agent-hardcoded/

# Manager's manual (kept local, regenerated from source)
AGENT_MANUAL.md
AGENT_MANUAL.docx
build_manual_docx.py
# Engineering manual: source builder lives in agent-hardcoded/ (already
# ignored); these are the generated .docx/.pdf outputs (EN + ZH, incl. A5).
Kofon_Chatbot_Engineering_Manual*.docx
Kofon_Chatbot_Engineering_Manual*.pdf

# planning
conversation_analysis.md

